import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        marginHorizontal: 0,
        marginBottom: 0,
    },

    swipeableArea: {
        flex: 1,
    },

    contentArea: {
        flex: 1,
    },
});
